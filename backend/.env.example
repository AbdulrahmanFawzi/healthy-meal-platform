# ============================================
# HEALTHY MEAL PLATFORM - Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit the actual .env file to git

# ============================================
# Server Configuration
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# Database Configuration (MongoDB)
# ============================================
# Local Development:
MONGODB_URI=mongodb://localhost:27017/healthy-meal-platform

# Production (MongoDB Atlas):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/healthy-meal-platform

# ============================================
# JWT Authentication
# ============================================
# Secret key for signing JWT tokens
# IMPORTANT: Use a long, random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-crypto-random

# Token expiration time (e.g., "7d", "24h", "60m")
# Using 7 days for development convenience
JWT_EXPIRES_IN=7d

# ============================================
# Image Upload (Cloudinary)
# ============================================
# Sign up at https://cloudinary.com for free account
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

# ============================================
# CORS Configuration
# ============================================
# Frontend URLs allowed to make API requests
# Comma-separated list
ALLOWED_ORIGINS=http://localhost:4200,http://localhost:5000
